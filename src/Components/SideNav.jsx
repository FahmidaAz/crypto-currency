import React from 'react';

const SideNav = () => {
    const SideNav = () => (
        <div className="sidenav">
          <h1>here is my side nav bar</h1>
        </div>
      );
    return (
        <div>
            
        </div>
    );
};

export default SideNav;